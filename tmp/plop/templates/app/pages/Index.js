import { Heading } from 'grommet'
import React from 'react'

export default () => (
  <div>
    <Heading><code>{{ name }}</code> app</Heading>
    <p>This app handles all some routes, i.e. all routes under <code>/[ADD ROUTES HERE]</code>.</p>
  </div>
)
