import { shallow } from 'enzyme'
import React from 'react'

import TumblrIcon from './TumblrIcon'

let wrapper

describe('Component > TumblrIcon', function () {
  before(function () {
    wrapper = shallow(<TumblrIcon />)
  })

  it('should render without crashing', function () {})
})
