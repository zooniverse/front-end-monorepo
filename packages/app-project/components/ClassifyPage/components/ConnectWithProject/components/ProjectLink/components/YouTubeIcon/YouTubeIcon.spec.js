import { shallow } from 'enzyme'
import React from 'react'

import YouTubeIcon from './YouTubeIcon'

let wrapper

describe('Component > YouTubeIcon', function () {
  before(function () {
    wrapper = shallow(<YouTubeIcon />)
  })

  it('should render without crashing', function () {})
})
