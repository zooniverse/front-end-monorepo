import { shallow } from 'enzyme'
import React from 'react'

import TwitterIcon from './TwitterIcon'

let wrapper

describe('Component > TwitterIcon', function () {
  before(function () {
    wrapper = shallow(<TwitterIcon />)
  })

  it('should render without crashing', function () {})
})
