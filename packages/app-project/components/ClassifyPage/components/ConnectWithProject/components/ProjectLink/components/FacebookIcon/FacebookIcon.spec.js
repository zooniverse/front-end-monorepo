import { shallow } from 'enzyme'
import React from 'react'

import FacebookIcon from './FacebookIcon'

let wrapper

describe('Component > FacebookIcon', function () {
  before(function () {
    wrapper = shallow(<FacebookIcon />)
  })

  it('should render without crashing', function () {})
})
