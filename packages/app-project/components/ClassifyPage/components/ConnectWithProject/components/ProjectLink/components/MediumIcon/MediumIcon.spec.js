import { shallow } from 'enzyme'
import React from 'react'

import MediumIcon from './MediumIcon'

let wrapper

describe('Component > MediumIcon', function () {
  before(function () {
    wrapper = shallow(<MediumIcon />)
  })

  it('should render without crashing', function () {})
})
