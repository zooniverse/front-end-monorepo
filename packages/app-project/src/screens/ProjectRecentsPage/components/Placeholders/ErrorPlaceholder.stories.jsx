import ErrorPlaceholder from './ErrorPlaceholder'

const meta = {
  title: 'Project App / Screens / Project Recents / ErrorPlaceholder',
  component: ErrorPlaceholder
}

export default meta

export const Default = {
  args: {}
}
