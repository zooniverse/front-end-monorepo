import { expect } from 'chai'

import TeamStore from './TeamStore'

describe('Stores > TeamStore', function () {
  it('should export an object', function () {
    expect(TeamStore).to.be.an('object')
  })
})
