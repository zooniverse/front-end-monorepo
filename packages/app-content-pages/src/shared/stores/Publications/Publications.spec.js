import { expect } from 'chai'

import Publications from './Publications'

describe('Stores > Publications', function () {
  it('should export an object', function () {
    expect(Publications).to.be.an('object')
  })
})
