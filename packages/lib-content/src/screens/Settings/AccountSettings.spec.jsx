import { render, screen } from '@testing-library/react'
import { composeStory } from '@storybook/react'

import Meta, { Default } from './AccountSettings.stories'

describe('Component > Settings > Account Settings Page', function () {
    it('test', function () {
      expect(true).to.equal(true)
    })
})
