# MetadataIconButton

An icon button component to view subject metadata.
Clicking the button opens a modal with metadata key/value rows.

## Props

- props pass through to the IconActionButton component
- `disabled` (bool): if true, the button is disabled
- `metadata` (object): the metadata to display in the modal
